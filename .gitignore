node_modules/
dist/
*.tsbuildinfo

# NEVER commit keys or passphrases
/.starling/
.starling/
*.keystore.json
.env
.env.*
!.env.example

# local-only operator utilities (machine paths; touch real wallets)
scripts/
